mongo.url=mongodb://mongo:27017/esn_docker
mongo.database=esn_docker
domains=open-paas.org
rest.api.port=8080
spa.calendar.url=https://e-calendrier.avocat.fr
spa.contacts.url=https://e-contacts.avocat.fr
spa.excal.url=https://excal.linagora.com
openpaas.backend.url=https://openpaas.linagora.com
self.url=https://twcalendar.linagora.com
dav.url=http://esn_sabre
dav.admin.user=admin
dav.admin.password=secret123
dav.rest.client.trust.all.ssl.certs=false
dav.rest.client.response.timeout=10000
# Whether to ignore quorum queues setting. Optional, boolean. Default to false. If `true`, Dav queues would use classic queues.
dav.queues.quorum.bypass=false
visio.url=https://jitsi.linagora.com
openpaas.backend.trust.all.certificates=false
jwt.key.private=file://conf/jwt_privatekey
jwt.key.public=file://conf/jwt_publickey
jwt.key.validity=PT12H
oidc.userInfo.url=http://keycloak:8080/auth/realms/oidc/protocol/openid-connect/token/userinfo
oidc.introspect.url=http://keycloak:8080/auth/realms/oidc/protocol/openid-connect/token/introspect
oidc.introspect.credentials=Bearer ewjiwelhwew
oidc.claim=email
oidc.audience=tcalendar
calendar.sharing.enabled=true
contacts.sharing.enabled=true
domain.contacts.enabled=true
default.language=en
default.timezone=Europe/Paris
default.business.hours.start=8:0
default.business.hours.end=19:0
default.business.hours.daysOfWeek=1,2,3,4,5,6
default.use.24h.format=true
jwt.technical.token.secret=technicalTokenSecret

## SMTP configuration
smtp.host=localhost
smtp.port=25
smtp.helo=localhost
smtp.ssl.enabled=false
smtp.ssl.trustAllCerts=false
smtp.starttls.enabled=false
# Uncomment and set these if authentication is needed
# smtp.username=your_username
# smtp.password=your_password

## OIDC configuration
# OIDC token cache expiration time (default: 5 minutes).
# Supports time units: s (seconds), m (minutes), h (hours), ...
oidc.token.cache.expiration=300s

# Max number of OIDC tokens to cache in memory (default: 10000).
# Ignored with Redis cache.
# oidc.token.cache.maxSize=10000

## File upload configuration
## Expiration time of an uploaded file. Optional, string. Default to 1h
# upload.expiration=1h
## Max upload limit per user. Optional, string. Default to 50MiB
# upload.user.total.limit=50 Mib

mail.template.location=file://root/resources/templates
mail.sender=no-reply@openpaas.org


basic.auth.enabled=true
admin.username=admin@open-paas.org
admin.password=secret